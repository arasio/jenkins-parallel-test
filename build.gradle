apply plugin: 'java'

repositories {
    jcenter()
}

dependencies {
    testCompile 'junit:junit:4.12'
}

test {
  def exclusions = new File('exclusions.txt')
  if(exclusions.exists()) {
    exclusions.eachLine { line ->
      exclude line
    }
  }
}
